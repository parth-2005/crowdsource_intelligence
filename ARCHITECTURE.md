# 🏗️ CrowdPulse Architecture Diagram

## 📊 Layer Overview

```
┌─────────────────────────────────────────────────────────────┐
│                     PRESENTATION LAYER                      │
│  ┌───────────────┐  ┌─────────────────┐  ┌──────────────┐  │
│  │ SplashScreen  │  │   HomeScreen    │  │   Widgets    │  │
│  │               │→ │  (BlocProvider) │  │ - CardView   │  │
│  │  3s Animation │  │                 │  │ - StatsOverlay│ │
│  └───────────────┘  └────────┬────────┘  │ - KarmaBadge │  │
│                              │            └──────────────┘  │
└──────────────────────────────┼─────────────────────────────┘
                               │
                               │ BlocBuilder/BlocListener
                               ↓
┌─────────────────────────────────────────────────────────────┐
│                      LOGIC LAYER (BLoC)                     │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              FeedBloc (State Machine)                │   │
│  │                                                      │   │
│  │  Events:           States:                          │   │
│  │  • LoadFeed    →   • FeedLoading                    │   │
│  │  • SwipeCard   →   • FeedLoaded                     │   │
│  │  • ResumeFeed  →   • StatsReveal  ⚠️ FREEZE UI     │   │
│  │                    • FeedError                      │   │
│  └─────────────────────────┬────────────────────────────┘   │
│                            │                                │
│  ┌─────────────────────────┴────────────────────────────┐   │
│  │              UserBloc (Gamification)                 │   │
│  │                                                      │   │
│  │  Events:           State:                           │   │
│  │  • AddKarmaPoints → UserState {                     │   │
│  │  • ResetKarma       karmaPoints, totalSwipes        │   │
│  │                   }                                 │   │
│  └─────────────────────────┬────────────────────────────┘   │
└────────────────────────────┼─────────────────────────────────┘
                             │
                             │ Calls Repository Methods
                             ↓
┌─────────────────────────────────────────────────────────────┐
│                       DATA LAYER                            │
│  ┌──────────────────────────────────────────────────────┐   │
│  │         ICardRepository (Interface)                  │   │
│  │  • Future<List<CardModel>> fetchCards()              │   │
│  │  • Future<void> submitSwipe(cardId, isYes)           │   │
│  └───────────────────────┬──────────────────────────────┘   │
│                          │                                  │
│                          │ Implements                       │
│                          ↓                                  │
│  ┌──────────────────────────────────────────────────────┐   │
│  │    MockCardRepository (Mock Implementation)          │   │
│  │  • 800ms delay simulation                            │   │
│  │  • 7 pre-populated cards                             │   │
│  │  • No actual network calls                           │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              Models (Data Classes)                   │   │
│  │  • CardModel (id, type, question, imageUrl, stats)   │   │
│  │  • StatsData (yesPercent, totalVotes)                │   │
│  │  • StatsModel (for overlay display)                  │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                             ↑
                             │ Injected via GetIt
                             │
┌─────────────────────────────────────────────────────────────┐
│              DEPENDENCY INJECTION (main.dart)               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  setupDependencies() {                               │   │
│  │    getIt.registerLazySingleton<ICardRepository>(     │   │
│  │      () => MockCardRepository()                      │   │
│  │    );                                                │   │
│  │    getIt.registerFactory<FeedBloc>(...);             │   │
│  │    getIt.registerFactory<UserBloc>(...);             │   │
│  │  }                                                   │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow - Swipe Action

```
User Swipes Card
      │
      ↓
┌─────────────────────────────────────────┐
│ CardSwiper.onSwipe()                    │
│ • Detects direction (left/right)        │
│ • Triggers HapticFeedback               │
└─────────────┬───────────────────────────┘
              │
              ↓
┌─────────────────────────────────────────┐
│ HomeScreen adds SwipeCard event         │
│ context.read<FeedBloc>()                │
│   .add(SwipeCard(cardId, direction))    │
└─────────────┬───────────────────────────┘
              │
              ↓
┌─────────────────────────────────────────┐
│ FeedBloc._onSwipeCard()                 │
│ 1. Get swiped card                      │
│ 2. Submit to repository                 │
│ 3. Calculate isMajority                 │
│ 4. emit(StatsReveal(...))               │
└─────────────┬───────────────────────────┘
              │
              ↓
┌─────────────────────────────────────────┐
│ HomeScreen BlocBuilder                  │
│ if (state is StatsReveal)               │
│   return StatsOverlayWidget(...)        │
│ ↓                                       │
│ UI FREEZES - Overlay takes full screen  │
└─────────────┬───────────────────────────┘
              │
              ↓
┌─────────────────────────────────────────┐
│ StatsOverlayWidget                      │
│ • Shows pie chart animation             │
│ • Displays badge (majority/minority)    │
│ • [If Golden Ticket] Lottie confetti    │
│ • "NEXT" button visible                 │
└─────────────┬───────────────────────────┘
              │
              │ User clicks "NEXT"
              ↓
┌─────────────────────────────────────────┐
│ FeedBloc.add(ResumeFeed())              │
└─────────────┬───────────────────────────┘
              │
              ↓
┌─────────────────────────────────────────┐
│ FeedBloc._onResumeFeed()                │
│ 1. Remove swiped card from stack        │
│ 2. emit(FeedLoaded(remaining cards))    │
└─────────────┬───────────────────────────┘
              │
              ↓
┌─────────────────────────────────────────┐
│ HomeScreen BlocBuilder                  │
│ Shows CardSwiper with next card         │
└─────────────────────────────────────────┘
```

---

## 🎯 State Machine - FeedBloc

```
       ┌──────────────┐
       │ FeedLoading  │ ← Initial State
       └──────┬───────┘
              │
              │ LoadFeed event
              │ (fetch from repository)
              ↓
       ┌──────────────┐
   ┌──→  FeedLoaded   │
   │   │ (cards list) │
   │   └──────┬───────┘
   │          │
   │          │ SwipeCard event
   │          │ (process swipe)
   │          ↓
   │   ┌──────────────┐
   │   │ StatsReveal  │ ⚠️ CRITICAL STATE
   │   │ (freeze UI)  │    • UI stops
   │   └──────┬───────┘    • Overlay shows
   │          │            • Stats animate
   │          │
   │          │ ResumeFeed event
   │          │ (remove card)
   └──────────┘
```

---

## 🧩 Component Relationships

```
┌────────────────────────────────────────────────────────┐
│                    CrowdPulseApp                       │
│                   (MultiBlocProvider)                  │
│  ┌──────────────────┐  ┌───────────────────────────┐  │
│  │   FeedBloc       │  │      UserBloc             │  │
│  │  (provided)      │  │     (provided)            │  │
│  └──────────────────┘  └───────────────────────────┘  │
│           │                       │                    │
│           └───────────┬───────────┘                    │
│                       │                                │
│                       ↓                                │
│              ┌─────────────────┐                       │
│              │  SplashScreen   │                       │
│              └────────┬────────┘                       │
│                       │                                │
│                       │ Navigate after 3s              │
│                       ↓                                │
│              ┌─────────────────┐                       │
│              │   HomeScreen    │                       │
│              │ (BlocConsumer)  │                       │
│              └────────┬────────┘                       │
│                       │                                │
│         ┌─────────────┼─────────────┐                  │
│         │             │             │                  │
│         ↓             ↓             ↓                  │
│  ┌───────────┐ ┌────────────┐ ┌─────────────┐         │
│  │ CardView  │ │StatsOverlay│ │ KarmaBadge  │         │
│  │  Widget   │ │   Widget   │ │   Widget    │         │
│  └───────────┘ └────────────┘ └─────────────┘         │
└────────────────────────────────────────────────────────┘
```

---

## 🔌 Dependency Injection Flow

```
main.dart
   │
   ├─ setupDependencies()
   │     │
   │     ├─ registerLazySingleton<ICardRepository>
   │     │     └─> MockCardRepository() [CREATED ONCE]
   │     │
   │     ├─ registerFactory<FeedBloc>
   │     │     └─> new FeedBloc(repository) [NEW EACH TIME]
   │     │
   │     └─ registerFactory<UserBloc>
   │           └─> new UserBloc() [NEW EACH TIME]
   │
   └─ runApp(CrowdPulseApp())
         │
         └─ MultiBlocProvider(
               providers: [
                 BlocProvider<FeedBloc>(
                   create: (_) => getIt<FeedBloc>() ← INJECTED
                 ),
                 BlocProvider<UserBloc>(
                   create: (_) => getIt<UserBloc>() ← INJECTED
                 ),
               ]
            )
```

---

## 📦 Package Architecture

```
Core Packages:
├─ flutter_bloc (9.1.1)
│   └─ Provides BlocProvider, BlocBuilder, BlocConsumer
├─ equatable (2.0.7)
│   └─ Used in all models, events, states for value equality
└─ get_it (9.2.0)
    └─ Dependency injection container

UI Packages:
├─ flutter_card_swiper (7.2.0)
│   └─ CardSwiper widget for Tinder-like interface
├─ animate_do (4.2.0)
│   └─ FadeIn, ZoomIn, etc. animations
├─ lottie (3.3.2)
│   └─ Confetti animation for Golden Tickets
├─ fl_chart (1.1.1)
│   └─ PieChart for stats visualization
└─ google_fonts (6.3.3)
    └─ Poppins typography
```

---

## 🎨 Theme Structure

```
AppTheme.darkTheme
├─ primaryColor: #673AB7 (Deep Purple)
├─ accentColor: #FFD700 (Gold)
├─ backgroundColor: #121212 (Dark)
├─ textTheme: Poppins Font Family
│   ├─ displayLarge (32px, bold)
│   ├─ headlineMedium (24px, w600)
│   ├─ titleLarge (20px, w600)
│   ├─ bodyLarge (16px, normal)
│   └─ ...
├─ cardTheme: Rounded 20px, elevation 8
└─ buttonTheme: Deep Purple, Poppins
```

---

## 🔧 Configuration Points

```
Customization Files:
├─ lib/core/theme/app_theme.dart
│   └─ Colors, fonts, theme settings
├─ lib/core/constants/app_constants.dart
│   └─ Karma points, durations, thresholds
├─ lib/data/repositories/mock_card_repository.dart
│   └─ Mock data cards
└─ lib/data/repositories/i_card_repository.dart
    └─ Interface for backend integration
```

---

**This architecture ensures:**
- ✅ Separation of Concerns
- ✅ Testability (mock repository)
- ✅ Scalability (interface-based)
- ✅ Maintainability (clear layers)
- ✅ Clean Code principles
